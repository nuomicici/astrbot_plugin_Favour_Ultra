# 更新须知
更新后如果无法正常加载，请依次尝试：
1. 重启框架
2. 重新安装该插件
3. **必看！**：如果后台显示"无法识别好感度标签"，请先尝试在预设对话中添加如下内容并清空历史对话记录！（若无效请前往反馈）
```
- 你好！
- 你好呀~[好感度 持平]
- 早上好！
- 早上好[好感度 持平]
```
4. 如遇问题，可以复制下方链接，于 手动下载 -> 链接下载 处手动回退
https://github.com/nuomicici/astrbot_plugin_Favour_Ultra/releases/tag/v4.3.0

## 更新日志
### v4.4.4 (2026-08-30)
- **优化注入 prompt，节省 token 和加强输出标签限制**：
  - **静态系统指令瘦身**：精简 XML 包装层级与冗余说明，Token 占用降低约 45%，显著缓解对主 Persona 人设的注意力稀释，加快 LLM 首字生成速度。
  - **解决多轮对话漏标痛点**：在动态上下文（`<FavourContext>`）末尾新增 `<MandatoryFooter>` 尾部近因强化约束，大幅提升模型在长上下文下输出好感度日志标签的稳定性。
  - **规范化单行关系定义与示例**：提炼通用关系冲突规则，并在关系标签规则中补充单行同意/拒绝示范，兼顾大模型精简性与弱模型（7B/14B）的格式精准度。
  - **人设优先与作用域隔离**：调整提示词注入顺序为人设在前、机制在后，并使用 `<ScoringMode>` 明确评分规则作用域，防止干扰 Bot 人设语气。
### v4.4.3 (2026-08-28)
- **修复插件更新/重启后配置可能清空的问题**：
  - 根因：配置写入采用直接覆盖写，更新/重启时进程若在写入中途被杀，配置文件被截断成非法 JSON，下次加载失败即用默认配置覆盖，导致 WebUI 配置全部丢失
  - 修复：配置写入改为「写临时文件 + `os.replace` 原子替换」，写入中途被杀不再产生损坏文件
  - 配置文件损坏时先备份为 `.corrupt_backup`，再尝试从 `backups/` 下最近的配置备份恢复，找不到才用默认配置
- **WebUI 配置添加/删除项丢失未保存数据**：
  - 根因：在分级/衰减规则/搭话规则/列表项（特使、黑白名单）点「+ 添加」或「× 删除」时，会先改内存配置再重新渲染整个面板，而输入框的值只在「保存」时才回写，未失焦的已输入内容会随重渲染丢失
  - 修复：添加/删除动态项、衰减模式切换、Tab 切换前先收集当前输入框（含未失焦）回写内存配置，重渲染后数据保留
  - 自动备份调度器现顺带备份配置文件到 `backups/`，便于配置损坏时恢复
### v4.4.2 (2026-08-28)
- **主动搭话直接发送路径注入对话历史上下文**：此前非 aiocqhttp 平台走 `_send_direct_active_chat` 直接调用 LLM 时，`llm_generate` 未传入 `contexts`，导致搭话只看到 persona + 搭话指令、不知道之前聊了什么，内容容易突兀
  - 现读取会话当前对话历史（过滤框架内部 checkpoint，截取最近 20 轮）转为 `list[Message]` 注入 `contexts`，搭话与前后文连贯
  - aiocqhttp 合成事件路径与旧版 `llm_manager.get_response` 路径无需改动（前者走完整管线自动注入，后者会自动读会话历史）
- **搭话发送前去除好感度标签，历史标签策略调整**：因本次注入上下文后模型会学习历史，需避免历史里反复出现"无标签搭话"导致模型停止输出标签
  - 发送给用户前自动用 `favour_pattern` 去除搭话内容中的好感度标签，用户只看到干净文本
  - 历史记录：LLM 输出**已含**标签 → 保留原始含标签文本，不再补标（模型已学会格式）；LLM 输出**不含**标签 → 补 `[好感度 持平]`，保持历史格式一致并提示模型格式
### v4.4.1 (2026-08-27)
- **主动搭话适配所有支持主动消息的平台**：
  - 新增 `_platform_supports_proactive` 判断，参考框架内置工具 `send_message_to_user`，统一由 `context.send_message` 平台无关路由
  - 明确不支持主动消息的平台（微信公众号、企微客服模式）触发时跳过实际发送并记录 warn，避免框架 raise
  - 搭话触发日志新增平台信息，便于跨平台排查
- **主动搭话内容写入对话历史**（conversation history），用 `[系统主动搭话触发]` 标记的 user 消息 + assistant 搭话原文成对写入，与正常用户消息区分
- **README 新增主动搭话平台支持说明**，列出全部平台的主动消息支持情况
- 调度器首轮等待缩短为启动后 1 分钟；跳过类日志降为 debug，仅保留触发/结果 warn
- 直接发送路径适配 v4.5.7+ SDK（`llm_generate` + `get_current_chat_provider_id`），修复 `llm_manager` 已废弃导致发送失败
### v4.4.0 (2026-08-27)
- **修复主动搭话在私聊场景下从未触发的问题**，主要原因与修复如下：
  1. 默认搭话规则最低只覆盖好感度 50，导致好感度不足 50 的用户无规则匹配、概率恒为 0 → 现已新增 0~49 区间兜底规则（概率 1%），并对老配置自动补全缺失的低好感度区间
  2. 会话白/黑名单此前为精确匹配，用户填写纯 QQ 号（如 `1090007836`）或平台前缀时无法命中完整 UMO（如 `aiocqhttp:FriendMessage:1090007836`）→ 现支持精确匹配 + 段尾缀匹配，纯 QQ 号即可生效
  3. 事件引用此前仅在 `on_llm_request` 钩子缓存，消息未触发 LLM（被指令/其他插件拦截）时不会缓存，导致搭话时无可用合成事件模板而被跳过 → 现新增 `event_message_type` 钩子尽早缓存，并兼容全局好感度模式
  4. 调度器首次执行需等待一个完整间隔（默认 2 小时）才跑首轮，启用后难以验证是否生效 → 首轮缩短为启动后 1 分钟，便于用户快速验证
- 主动搭话新增诊断日志：好感度未命中规则、会话被白名单过滤等情况会输出 INFO/DEBUG 日志，便于排查
### v4.3.0 (2026-07-24)
- 在现有配置中心 WebUI 新增「迁移同步」分页（不重写原 Pages）
- 支持按 UMO 一键复制/迁移会话数据（user_id、好感度、关系、唯一性、昵称等；仅 session_id 替换）
- 支持会话完全同步：配置 A/B 两 UMO 后双向实时同步变更（含 WebChat `/api/v1/chat` 会话）
- 同步对支持立即全量 merge（A→B / B→A / 双向）
### v4.2.2 (2026-07-22)
- 合并 [#18](https://github.com/nuomicici/astrbot_plugin_Favour_Ultra/pull/18)：`/修改关系`、`/全局修改关系` 的 `is_unique` 默认 0，可省略
- 修复 v3→v4 迁移时 min/max 好感度边界与分级错配（#19）
- 修复备份配置为 null 时自动备份崩溃（#19）
- 主动搭话：支持会话黑白名单与每轮会话上限；修复触发计数错误  
  注意：主动搭话为实验性功能，请勿用于正式生产环境
- 修复取消冷暴力时用户 ID 子串误匹配
- 修复主动搭话长文本软分段在超长段上失效
- 修复 Windows 下备份文件名含 `:` 等非法字符导致失败
- 调整「全局好感度」：同一适配器实例内共享，不同适配器实例独立计数
- 查询/数据管理逻辑同步适配新的共享会话标识（兼容旧 `global` 记录）
### v4.2.1 (2026-07-09)
**修复**
- 修复当[`好感度不处于已设置等级时，不告知LLM应如何应对`](https://github.com/nuomicici/astrbot_plugin_Favour_Ultra/issues/16)的问题
### v4.2.0 (2026-07-09)
**新增功能**
- **数据库自动备份系统**：支持定时自动备份（可配置间隔与保留时长），WebUI「备份」Tab 提供创建/恢复/删除/下载备份的一站式管理
- **WebUI 暗色模式**：CSS 全面重构，采用 CSS 自定义属性 + `data-theme="dark"` 暗色主题，配色调整为 AstrBot 兼容蓝色系，使用系统字体替代 Google Fonts
- **数据 Tab 增强**：支持简要/详细双视图切换、SID 关键字搜索过滤、按适配器平台分组展示

**健壮性增强**
- **SQLite 并发优化**：启用 WAL 日志模式 + `busy_timeout=30000`，配置连接池 (`pool_size=1, max_overflow=2`)，新增 `_retry_on_locked` 装饰器对所有写操作提供指数退避重试（最多 3 次），大幅降低高并发场景下 `database is locked` 错误

**Bug 修复**
- 修复自动拉黑时未调用 `event.stop_event()` 导致消息仍在传播的问题
- 修复非 QQ 平台搭话 `_send_direct_active_chat` 缺少 `await` 的问题

**性能优化**
- 新增用户名缓存 (`_username_cache`)，避免每条消息都触发数据库写入以更新用户名

**UI 调整**
- 配置面板标题与保存按钮文案精简
- 状态徽章初始态改为 `loading`

### v4.1.0 (2026-06-02)
**Bug 修复**
- 缺少 `terminate()` → 热重载后旧调度器泄漏，多实例并行
- 非 QQ 平台 `AiocqhttpMessageEvent` 硬导入 → 启动崩溃（改延时导入）
- `_reload_config_from_manager` 漏掉 `active_chat`/`favour_decay` 字段 → WebUI 关闭搭话不生效
- WebUI 保存后 DB 边界 / 权限阈值未热更新
- 微信搭话：合成事件不被适配器识别 → 非 QQ 平台改直调 LLM + 分段发送
- 搭话调度器无事件引用跳过 → 加平台级缓存兜底

**过时清理**
- `favour_rule_prompt` 死字段：从 `main.py` / `config_manager.py` / `_conf_schema.json` / `config.json` 全部移除

**新增功能**
- `terminate()` 插件卸载时取消调度器
- WebUI「数据」Tab：非全局按会话分组展开 + 全局表格，搜索过滤，在线编辑好感度/关系/用户名/唯一性，保存/删除（沙箱兼容二次确认）
- 数据库 `username` 字段 + 自动采集
- `get_all_records` / `update_record` / `delete_record` API

**交互优化**
- 查询权限：批量查询加权限校验，WebUI 复选框改下拉（群/私聊分开）
- 删除按钮二次点击确认（替代被沙箱拦截的 `confirm()`）
- 数据页分组标题展示完整会话 UID

### v4.0.0（2026-06-01）
> 大重构，需要检查一下新的配置
#### 功能更新
1.  **全新 WebUI 配置面板**：新增可视化配置面板，所有参数均在webui修改，优化了视觉效果
2.  **自定义分级系统**：新增 `favour_levels` 配置，支持自定义等级名称、数值区间与专属行为规则。Prompt 将根据动态数据仅注入当前所处的等级规则，大幅节省 Token 消耗
>（⚠️ **注意：更新后旧版规则将失效，请务必前往面板重新配置一次！**）
3.   **长期无互动衰减**：引入好感度衰减机制。支持“线性”与“分级”双模式。当无互动超过设定天数时，后台异步调度器会自动扣减好感度，无需人工干预。
4.   **自动拉黑**：当好感度跌至设定的最低阈值时，将触发自动拉黑，不再响应并监听该用户的任何消息。
5.   **主动搭话**：支持按好感度区间独立配置主动搭话概率。搭配自定义时间窗口、检查间隔及特定的 LLM 搭话提示词，让互动更加自然。
6.   **查询控制**：新增 `query_permission` 字段，可独立开关群聊/私聊环境下普通用户对好感度的查询权限。可选择，让你家机器人更像个人（大概吧
7.   **LLM 自主关系管理**：赋予 AI 更高的主导权。
     -   **主动断交**：遭遇严重冒犯或好感跌破底线时，LLM 可使用 `[主动解除关系:目标用户ID]` 标签主动绝交。
     -   **主动结缘**：在情感自然发展到位时，LLM 可极度克制地使用 `[主动确认关系:目标用户ID:关系名称:排他性]` 主动确认羁绊关系。
     -   关系标签升级为支持指向第三方 ID 的 5 字段新格式，向下兼容旧版。所有 4 种关系指令标签现已在输出链中自动过滤，保证用户界面整洁隐形。

#### 架构优化
*   **动静分离的 Prompt 注入**：放弃全量构建机制，改为 `固定规则 + 动态数据上下文` 双通道分离注入。节省token，优化缓存命中效果。
*   **Prompt 全中文化与压缩**：大幅精简了冗长措辞，并将英文指令全面替换为纯中文，更利于国产中文大模型的理解。
*   **智能标签容错**：好感度标签正则修改。新增英文兜底（如 `[Favour increased:X]`）并支持中文书写容错（如 `[好-感度 降低]` 也能被正确识别），大幅降低 AI 幻觉导致的解析失败的概率。
*   **旧配置迁移**：首次加载会自动检测并迁移旧版框架中的配置文件，实现免动手无缝升级。

### v3.1.6（2026-04-16）
*   修复因出现 ``[` 字符后会导致误清洗内容的 bug。

### v3.1.3（2026-03-13）
*   **Prompt 更新**：使用更规范的 XML 格式注入，并要求达到上限后不再提高好感度防止输出出现错误。
*   如遇问题，可以复制下方链接，于 手动下载 -> 链接下载 处手动回退
```
https://github.com/nuomicici/astrbot_plugin_Favour_Ultra/tree/backuup/
```

### v3.1.2（2026-03-10）
*   **命令补全**：添加多个管理命令，方便管理员全局管理数据。
*   **冷暴力焕新**：将判定方式修改为好感度连续降低触发。
*   **输出优化**：添加分页功能，防止 T2I 输出过长图片导致无法正常发送。
*   **显示优化**：查看单人好感度直接文本输出，减少 T2I 负载。

### v3.1.0（2026-01-09）
*   **架构升级**：弃用 JSON 文件存储，全面迁移至 SQLite 数据库。
*   **配置重构**：引入嵌套配置结构，新增 `enable_cold_violence` 开关。
*   **功能增强**：
    *   新增 `跨会话修改` 命令，支持远程运维。
    *   优化 `查询全部好感度` 逻辑，支持按会话分组并智能隐藏私聊数据。
    *   多人查询命令全面适配 T2I 图片输出，表格化展示更清晰。
*   **修复**：修复了部分全局命令鉴权失败的问题。

### v3.0.2（2025-12-27）
*   优化冷暴力 Prompt 注入逻辑，防止 LLM 误学习上下文。

### v3.0.0（2025-12-05）
*   新增关系唯一性判断（如：只能有一个"恋人"）。
*   优化 T2I 输出容错率。
